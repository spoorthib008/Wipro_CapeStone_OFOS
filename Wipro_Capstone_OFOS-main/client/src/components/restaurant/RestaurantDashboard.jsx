// once the admin accept the registration request, the restaurant can access their profile and add menu, edit and delte
// get incomming and list of orders and can update each orders status (preparing, out for delivery and delivered)

import React from 'react'

const RestaurantDashboard = () => {
  return (
    <div>RestaurantDashboard</div>
  )
}

export default RestaurantDashboard